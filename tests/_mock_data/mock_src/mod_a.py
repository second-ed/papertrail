def func_a(a: float, b: float) -> float:
    return a + b


def func_b(a: float, b: float) -> float:
    """Simple docstring

    Args:
        a (float)
        b (float)

    Returns:
        float
    """
    return a - b
